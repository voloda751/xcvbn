from tkinter import Button


class Title():
    def __init__(self, title,x,y,view)g        self title = title
        self.x = x
        self.y =y
        self.viem =view
    def info(self)
        print("інформація:,self.title,self.x,self.y")
    def show(self):
        self.view = True
    def hide(self):
        self.view = False
        print(self.text,x,y, is_click):
    class Button(widget):
        def __init__(self,text,x,y,is_click):
            super().__init__(text,x,y)
            self.is_click = is_click

        def click(self):
            print("молодець")
            self.is_click = True
adolf = Button("я адольф", 56,14 False)
adolf.print_info()
a = input("")